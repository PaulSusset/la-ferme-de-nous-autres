{
  "private": true,
  "workspaces": [
    "workspace-a",
    "workspace-b"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "client": "cd client/ && yarn start",
    "server": "cd server/ && yarn start",
    "start": "npm-run-all --parallel client && server"
  }
}
